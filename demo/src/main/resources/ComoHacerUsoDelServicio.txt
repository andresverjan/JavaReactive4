En una consola correr

./gradlew build
./gradlew bootRun --info


Sobre la carpeta resources correr

docker-compose up

Para las pruebas en la carpeta resources se tiene la coleccion de postman
con peticiones correctas y que generan el error controlado.

